{% set name = "jupyterlab_sos" %}
{% set version = "0.10.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: c2b824f01fb1fbc8091803a03b096bc772712d0c60e0543fbfc82e7713eb0694

build:
  number: 0
  noarch: python

requirements:
  build:
    - nodejs
  host:
    - jupyter-packaging
    - pip
    - python >=3.6
  run:
    - jupyterlab >=3.0.0
    - jupyterlab-transient-display-data
    - nodejs
    - python >=3.6
    - sos

outputs:
  - name: jupyterlab_sos
    build:
      script: python -m pip install . --no-deps --ignore-installed -vv
    requirements:
      build:
        - nodejs
      host:
        - jupyter-packaging
        - pip
        - python >=3.6
      run:
        - jupyterlab >=3.0.0
        - jupyterlab-transient-display-data
        - nodejs
        - python >=3.6
        - sos
    test:
      commands:
        - jupyter labextension list
        - jupyter lab build
        - jupyter labextension list 1>labextensions 2>&1  # [unix]
        - grep "jupyterlab-sos.*enabled.*OK" labextensions  # [unix]

  - name: jupyterlab-sos
    requirements:
      run:
        - {{ pin_subpackage('jupyterlab_sos', exact=True) }}
    test:
      commands:
        - jupyter labextension list

about:
  home: https://github.com/vatlab/jupyterlab-sos
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: A JupyterLab extension for SoS Polyglot Notebook and Workflow System
  dev_url: https://github.com/vatlab/jupyterlab-sos

extra:
  recipe-maintainers:
    - BoPeng
    - izahn
